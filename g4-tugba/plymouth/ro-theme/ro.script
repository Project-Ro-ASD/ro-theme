screen.SetBackgroundTopColor(0.05, 0.05, 0.08);
screen.SetBackgroundBottomColor(0.05, 0.05, 0.08);

logo = Image("logo.png");
sprite = Sprite(logo);

sprite.SetX(Window.GetWidth() / 2 - logo.GetWidth() / 2);
sprite.SetY(Window.GetHeight() / 2 - logo.GetHeight() / 2);
